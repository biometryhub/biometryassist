% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variogram.r
\name{vario_interp}
\alias{vario_interp}
\title{Interpolate variogram points onto a regular grid}
\usage{
vario_interp(points, n = 40)
}
\arguments{
\item{points}{A data frame of variogram points for a single group. The
spatial coordinates are expected in the first two columns (column 1 maps to
\code{y}, column 2 to \code{x}) and the semivariance in a \code{gamma} column.}

\item{n}{The number of grid points along each axis (default \code{40}).}
}
\value{
A data frame with columns \code{x}, \code{y} and \code{z} (the interpolated
\code{gamma}), containing \code{n * n} rows.
}
\description{
Internal helper for \code{\link[=variogram]{variogram()}}. Takes the variogram points for a single
group (as produced by \code{\link[=vario_df]{vario_df()}}) and interpolates the \code{gamma} surface onto
a regular \code{n} by \code{n} grid using \code{\link[pracma:interp2]{pracma::interp2()}}. The returned grid is the
shared input to both the 2D heatmap (\code{\link[=vario_ggplot]{vario_ggplot()}}) and the 3D wireframe.
}
\keyword{internal}
