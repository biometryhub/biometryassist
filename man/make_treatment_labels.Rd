% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{make_treatment_labels}
\alias{make_treatment_labels}
\title{Build per-row treatment labels from one or more factor columns}
\usage{
make_treatment_labels(pp, vars, sep)
}
\arguments{
\item{pp}{A predictions data frame.}

\item{vars}{Character vector of factor column name(s) to combine.}

\item{sep}{Separator used to join the columns when \code{vars} has length > 1.}
}
\value{
A vector of labels, one per row of \code{pp}.
}
\description{
Shared by \code{\link[=multiple_comparisons]{multiple_comparisons()}} and \code{\link[=pairwise_comparisons]{pairwise_comparisons()}} to turn the
classify factor column(s) of a predictions data frame into a single label per
row. A single factor is returned as-is (preserving its type); multiple
factors (an interaction) are joined with \code{sep}. The caller chooses the
separator (\code{multiple_comparisons()} uses \code{"_"}, \code{pairwise_comparisons()} uses
\code{":"}) and any further processing (e.g. coercion to character).
}
\keyword{internal}
