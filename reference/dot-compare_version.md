# Function to compare package version for mocking

Function to compare package version for mocking

## Usage

``` r
.compare_version(a, b)
```

## Arguments

- a, b:

  Character strings representing package version numbers.

## Value

Numeric. `0` if the numbers are equal, `-1` if `b` is later and `1` if
`a` is later
